  <div>
  <%= form_with(model:[@diary, @dia_comment], local: true, remote: true) do |f| %>
    <%= f.text_area :comment, rows:'5',placeholder: "Comment" %>
    <%= f.submit "送信" %>
  <% end %>
  </div>